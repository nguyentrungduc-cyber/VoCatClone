package com.vocatclone.app

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Test mẫu để xác nhận môi trường build/test hoạt động đúng.
 * Chạy: ./gradlew test
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
